# UI Models

Reserved for shared UI-facing models and projection shapes that are useful across application surfaces.

Rules:

- do not let UI models become the domain source of truth
- keep them derived from explicit platform contracts and read models
