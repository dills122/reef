# Packages

This area holds reusable internal libraries and shared non-service-specific assets.

Planned subareas:

- `ui-models/` for shared frontend-facing models if needed
- `scenario-definitions/` for reusable scenario assets and definitions
