# Contracts

This area holds shared inter-service contracts and schemas.

Initial intent:

- define runtime-to-engine request and response contracts
- evolve toward protobuf-backed versioned contracts
- keep commands and events explicit

Current subareas:

- [`proto/`](./proto/)
