---
title: Reef
description: Simulation-first institutional trading venue and post-trade platform.
template: splash
banner:
  content: |
    Pre-release, heavy development. Reef is already runnable, but APIs, schemas, and game mechanics still change - see <a href="overview/status/">Current Status</a>.
hero:
  tagline: "A hands-on market-infrastructure lab: submit orders, watch them match, replay what happened, and follow trades into settlement."
  actions:
    - text: What Is Reef
      link: overview/what-is-reef/
      icon: right-arrow
      variant: primary
    - text: Trading API
      link: api/overview/
      icon: document
    - text: Bot Arena (WIP)
      link: arena/overview/
      icon: rocket
---

import { Card, CardGrid, Badge, Aside } from '@astrojs/starlight/components';

Reef is built to make market plumbing visible. Orders go through a real command boundary, hit a hidden-book matching engine, produce trades, then leave evidence you can inspect and replay. The platform is shaped like serious infrastructure, but it stays local-first so you can run it, break it, and understand it from end to end.

<div style="display: flex; flex-wrap: wrap; gap: 0.5rem; margin: 1rem 0;">
	<Badge text="Kotlin runtime" variant="note" />
	<Badge text="Go matching engine" variant="note" />
	<Badge text="Postgres" variant="note" />
	<Badge text="Protobuf contracts" variant="note" />
	<Badge text="Settlement facts: first slice" variant="note" />
	<Badge text="Bot Arena: WIP" variant="caution" />
</div>

## Explore The Docs

<CardGrid>
	<Card title="Project Overview" icon="open-book">
		The short version: what Reef is, who it is for, and why simulation drives the same command path as real users. Start at [What Is Reef](overview/what-is-reef/).
	</Card>
	<Card title="Bot Arena & Bot SDK" icon="rocket">
		The trading-bot game taking shape on top of the venue. Author a bot, run seeded matches, and keep every result replayable. Start at [Arena Overview](arena/overview/).
	</Card>
	<Card title="Trading API" icon="setting">
		The public surface for submitting orders, reading market data, checking command status, and inspecting settlement evidence. Start at [API Overview](api/overview/).
	</Card>
	<Card title="Data Schema" icon="document">
		Where Reef keeps durable facts, rebuildable projections, auth/admin policy, and wire contracts. Start at [Schema Overview](schema/overview/).
	</Card>
</CardGrid>

## How It Fits Together

Two layers, one command path. The **core platform** owns the business behavior: order intake, matching, trades, settlement evidence, and audit trails. The **simulation control plane** supplies scripted scenarios, participant bots, seeded randomness, and Bot Arena runs. Both layers use the same public commands, so a replayed scenario and a manual user leave comparable evidence.

```text
order intake -> hidden-book matching engine -> executions & trades
             -> settlement obligations/facts -> ledger proof -> audit trail
```

<Card title="Read The Architecture" icon="puzzle">
	How the runtime, matching engine, simulator, stock-data service, and schema boundaries fit together. See [Architecture](overview/architecture/).
</Card>

## Write A Trading Bot

Bots are TypeScript classes extending `ReefBotV1`. The harness handles ticks, validation, and risk checks; your bot reads the market snapshot and chooses what orders to place.

```ts
export default class ExampleBot extends ReefBotV1 {
  static override metadata = {
    name: "example-bot",
    publisher: "Example Publisher",
    email: "bot-author@example.com",
    version: "1.0.0",
    sdkVersion: "1.5.0",
    botApiVersion: "v1",
  } as const;

  override async onTick(ctx: BotContextV1) {
    const snapshot = await ctx.marketData.snapshot("AAPL");
    if (!snapshot.ok) return [];

    return [
      ctx.orders.placeLimit({
        instrumentId: "AAPL",
        side: "BUY",
        quantity: 10,
        limitPrice: snapshot.value.midPrice - 1,
      }),
    ];
  }
}
```

<Card title="Write your first bot" icon="rocket">
	Build a bot, run it locally, and see what the harness accepts or rejects. See [Bot SDK Quickstart](arena/bot-sdk-quickstart/).
</Card>

## Run It Locally

```bash
make dev-up      # start Postgres, runtime, matching engine
make dev-smoke    # verify the local stack end to end
make dev-sim      # drive a seeded scenario
```

<Aside type="caution" title="Pre-release">
	Reef is under heavy development. These docs describe the current build plus near-term shape, not a stable product release. See [Current Status](overview/status/) for what is built today.
</Aside>
